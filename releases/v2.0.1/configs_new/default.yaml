experiment_name: 'sparseinst'
run_name: 'test' # 'bf_tiled'
comment: '[cyto_nuc]-[bf]-[cond_pc]-[bce_l1_lpips]'
runs_dir: 'runs'
exist_ok: False

# Train settings -------------------------------------------------------------------------------------------------
epochs: 1
n_folds:  5
imgsz: 512
bs: 1
lr: 1e-4
wd: 1e-5
augment:  True

# Model settings -------------------------------------------------------------------------------------------------
arch:  'sparseinst'
in_channels:  1
num_masks:  25

# Scheduler settings -------------------------------------------------------------------------------------------------
scheduler: CosineAnnealingLR
min_lr: 1e-6
T_max: 55
T_0: 25
warmup_epochs: 10

# Other settings -------------------------------------------------------------------------------------------------
seed: 3407
device: cuda:1

